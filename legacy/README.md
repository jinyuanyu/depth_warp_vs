# Legacy

This directory stores archived or superseded root-level entry scripts that are kept for reference.

They are preserved to avoid losing historical implementations, but they are not the recommended entry points for current experiments.

For current inference, prefer:

- `reduced_core/depth_warp_vs/main.py`
- `scripts/train_refiner.py`
