# Tools

This directory contains utility scripts that were previously scattered at the repository root.

- `analysis/`: one-off analysis and visualization helpers
- `debug/`: manual debugging scripts
- `depth/`: standalone depth-related utilities
- `streaming/`: UDP streaming helpers
- top-level `tools/*.py`: general-purpose helper scripts

These scripts are not part of the minimal `reduced_core/` inference path.
